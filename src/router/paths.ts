const paths = {
    UPLOAD_PATH: "/optimizer/upload",
    DASHBOARD_PATH: "/optimizer/dashboard",
    PARAMEDITOR_PATH: "/optimizer/scenario",
    CONSTRAINTS_PATH: "/optimizer/generateupload",
}

export default paths;